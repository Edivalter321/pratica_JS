/* Crie uma função que receba um parametro idade e verifique se uma pessoa pode dirigir com base na idade
let idade = 20
function direitoDeDirigir() {
    if( idade >= 18) {
        console.log(`Você está apto(a) a dirigir!`)
    } else {
        console.log(`Você não não tem idade o suficiente para dirigir`)
    }
}
direitoDeDirigir(idade)
console.log(`${idade} anos`)
*/

/* Escreva uma função que receba dois parametros e calcule a média de notas de alunos e atribuir um conceito

 function mediaNotas (nota1,nota2) {
    let media = (nota1 + nota2) / 2
    if (media > 7 && media < 10) {
        console.log("aprovado")
    } else if (media < 7 && media > 6) {
        console.log("Conceito B")
    } else if (media < 6 && media > 4) {
        console.log("Conceito AF")
    } else {
        console.log("Reprovado")
    }
    return media
    
 }
 mediaNotas(5,5)
 */
 /*function calcularImc (altura,peso) {
    let imc = peso / (altura * altura)
    if (imc < 18.5) {
        console.log("Desnutrição")
    } else if (imc > 18.5 && imc < 24.9) {
        console.log("Eutrofico")
    } else if (imc > 24.9 && imc < 29.9) {
        console.log("Sobrepeso")
    } else {
        console.log("Obesidade")
    }
 }
 calcularImc(1.80,90)
*/
  /*  let comidas = ["panelada", "vitamina", "arroz", "feijao"]
    comidas.push("doce")
    console.log(comidas)
    console.log(comidas.length)
    comidas.shift()
    console.log(comidas)
    */
  /* let numeros = [10,12,43,2]
   numeros.push(20)
   console.log(numeros)
   numeros.push(30,40,11)
   console.log(numeros)
   */